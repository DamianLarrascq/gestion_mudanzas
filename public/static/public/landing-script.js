// Archivo script.js para la landing page
// Actualmente las animaciones son en CSS puro, así que no hay funciones JS activas.

document.addEventListener('DOMContentLoaded', () => {
    console.log("Landing page cargada y lista.");
    
    // Acá podés sumar funciones futuras, como eventos de click 
    // o un menú hamburguesa para mobile.
});